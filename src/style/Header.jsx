import styled from '@emotion/styled';

const Header = styled.header({
  backgroundColor: '#7d1917',
  textAlign: 'center',
  fontSize: 30,
  fontWeight: 700,
  padding: 20,
  '& a': {
    color: '#ddc2c2',
    textDecoration: 'none',
  },
});

export default Header;
