import styled from '@emotion/styled';

const Title = styled.h2({
  fontSize: '2em',
  margin: '2em 0 0 2em',
  padding: '.2em 0',
  color: '#FFABAB',
  borderBottom: '5px solid rgba(255,171,171,0.3)',
});

export default Title;
