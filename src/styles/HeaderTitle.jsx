import styled from '@emotion/styled';

const HeaderTitle = styled.span({
  '& a': {
    fontWeight: '700',
    fontStyle: 'italic',
    padding: '1em .5em',
    fontSize: '1.8em',
    color: '#2647DB',
    textDecoration: 'none',
  },
});

export default HeaderTitle;
