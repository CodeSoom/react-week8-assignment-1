import styled from '@emotion/styled';

const Form = styled.div({
  display: 'flex',
  flexWrap: 'wrap',
  alignItems: 'center',
  margin: 0,
  padding: '.4em 0',
  listStyle: 'none',
});

export default Form;
