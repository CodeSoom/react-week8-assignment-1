import styled from '@emotion/styled';

const RestaurantMenuItem = styled.li(() => ({
  width: '100%',
  marginBottom: '.1em',
  fontSize: '1em',
  fontWeight: 'bold',
  color: '#424242',
}));


export default RestaurantMenuItem;
