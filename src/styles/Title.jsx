import styled from '@emotion/styled';

const Title = styled.h2({
  fontSize: '1.8em',
  margin: 0,
  padding: '0.5em',
  border: '.4em double #424242',
  textAlign: 'center',
  color: '#424242',
});

export default Title;
