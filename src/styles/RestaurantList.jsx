import styled from '@emotion/styled';

const RestaurantList = styled.ul({
  display: 'flex',
  flexWrap: 'wrap',
  margin: 0,
  padding: '.4em 0',
  listStyle: 'none',
});

export default RestaurantList;
