import styled from '@emotion/styled';

const Button = styled.button({
  width: '35%',
  borderRadius: '.3em',
  border: 0,
  padding: '.7em',
  fontSize: '.9em',
  fontWeight: 'bold',
});

export default Button;
