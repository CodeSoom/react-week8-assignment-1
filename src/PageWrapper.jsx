import styled from '@emotion/styled';

const PageWrapper = styled.div({
  width: '70%',
  margin: '0 auto',
  '& h2': {
    margin: '20px auto',
    textAlign: 'left',
    color: '#364f6b',
    fontSize: '2em',
  },
});

export default PageWrapper;
