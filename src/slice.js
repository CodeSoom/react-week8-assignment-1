import { createSlice } from '@reduxjs/toolkit';

const slice = createSlice({
  name: 'application',
  initialState: {},
  reducers: {},
});

export const {
  actions,
  reducer,
} = slice;

export default reducer;
