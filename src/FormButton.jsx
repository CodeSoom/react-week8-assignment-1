import styled from '@emotion/styled';

const FormButton = styled.div({
  width: 'fit-content',
  border: 'none',
  backgroundColor: '#EEE',
  padding: '5px',
  borderRadius: '3px',
  cursor: 'pointer',
});

export default FormButton;
