import styled from '@emotion/styled';

const PageMain = styled.div({
  height: '550px',
  '& h2': {
    fontSize: '30px',
    textAlign: 'center',
    marginTop: '30px',
  },
});

export default PageMain;
